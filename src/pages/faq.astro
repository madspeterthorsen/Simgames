---
import Layout from "../layouts/Layout.astro";
import Footer from "../components/Footer.astro";
---

<Layout>
  <div class="cloud-layer">
    <img src="/sky.webp" class="cloud cloud-1" alt="" aria-hidden="true" />
    <img src="/sky.webp" class="cloud cloud-2" alt="" aria-hidden="true" />
    <img src="/sky.webp" class="cloud cloud-3" alt="" aria-hidden="true" />
  </div>



<!-- FULLSCREEN VIDEO -->
<section class="hero-video reveal">
  <video
    class="hero-video-el"
    src="/videos/konceptvideo.MOV"
    autoplay
    muted
    loop
    playsinline
  ></video>
</section>



  <!-- FAQ HEADER -->
 <section class="faq-header reveal">
    <h1>Koncept</h1>
    <h2>alt du skal vide om Simgames</h2>
  </section>

  <!-- FAQ CONTENT -->
  <section class="presentation-section reveal">
    <div class="info-box reveal">
      <h2>Ofte stillede spørgsmål</h2>

      <!-- FAQ ITEM -->
      <div class="info-item reveal">
        <div class="info-header">
          <span>Hvad er Simgames?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>
            Simgames er vores samling af minigames på golfsimulatoren – tænkt som et socialt univers, hvor I kan battle i alt fra target games og challenges til party-spil, der minder mere om arcade og gaming end klassisk golf. Det er skabt til
            venner, studiegrupper og firmaer, der vil have en anderledes aften med spil, drinks og god stemning.
          </p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Skal man kunne spille golf i forvejen?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Nej! Simgames er designet til både begyndere og øvede. Systemet hjælper jer med at komme i gang, og personalet kan altid give en hurtig intro til slag, køller og de forskellige spil.</p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Hvor mange kan vi være pr. simulator?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Som udgangspunkt anbefaler vi 4–6 personer pr. simulator for den bedste oplevelse.</p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Hvordan fungerer pakkerne med alkohol og Simgames?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Vores Simgames-pakker kombinerer spil på simulatoren med drikkepakker, så I får en samlet oplevelse.</p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Kan vi tilpasse gamemodes til vores event?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Ja, vi kan sammensætte et program, der passer til jer og jeres arrangement.</p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Hvordan booker vi Simgames?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Booking foregår via bookingsystemet på hjemmesiden.</p>
        </div>
      </div>

      <div class="info-item reveal">
        <div class="info-header">
          <span>Hvad skal vi have med – og hvad sørger I for?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>I skal bare møde op i behageligt tøj. Køller, bolde og teknikken står vi for.</p>
        </div>
      </div>

      <div class="info-item">
        <div class="info-header">
          <span>Kan vi kombinere Simgames med et større event eller pre-party?</span>
          <button class="toggle-btn">+</button>
        </div>
        <div class="info-content">
          <p>Ja, Simgames er oplagt som pre-party eller del af et større arrangement.</p>
        </div>
      </div>
    </div>
  </section>


  <!-- ACCORDION + CLOUD SCRIPT -->
  <script>
    // FAQ accordion
    document.querySelectorAll(".toggle-btn").forEach((btn) => {
      btn.addEventListener("click", () => {
        const item = btn.closest(".info-item");
        const isOpen = item.classList.contains("active");

        document.querySelectorAll(".info-item").forEach((i) => {
          i.classList.remove("active");
          const toggle = i.querySelector(".toggle-btn");
          if (toggle) toggle.textContent = "+";
        });

        if (!isOpen) {
          item.classList.add("active");
          btn.textContent = "−";
        }
      });
    });

    // Moving clouds
    (function () {
      const clouds = document.querySelectorAll(".cloud");
      if (!clouds.length) return;

      const maxMove = window.innerWidth + 600;

      window.addEventListener("scroll", () => {
        const progress = Math.min(window.scrollY / 600, 1);

        clouds.forEach((cloud, i) => {
          const direction = i % 2 === 0 ? -1 : 1;
          const offset = progress * maxMove;
          cloud.style.transform = `translateX(${direction * offset}px)`;
        });
      });
    })();
  </script>

  <!-- STYLING -->
  <style>
    * {
      box-sizing: border-box;
    }

    /* FULLSCREEN VIDEO */
    .hero-video {
      position: relative;
      max-width: 900px;
      margin: 0 auto;
      padding-top: 12rem;
      padding-bottom: 4rem;
      overflow: visible;
      z-index: 2;
    }

    .hero-video-el {
      width: 100%;
      max-height: 370px;
      object-fit: cover;
      display: block;
      border-radius: 15px;
      border: none;
      background: transparent;
      box-shadow: 0 0 40px rgba(255, 255, 255, 0.35);
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      pointer-events: none;
    }

    .hero-overlay h1 {
      color: white;
      font-size: 2.6rem;
      letter-spacing: 2px;
      text-align: center;
      text-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
    }

    /* FAQ HEADER */
    .faq-header {
      text-align: center;
      margin: 5rem 0 2rem;
      color: white;
      position: relative;
      z-index: 2;
    }

    .faq-header h1 {
      font-size: 2.6rem;
      margin-bottom: 0.5rem;
    }

    .faq-header h2 {
      font-size: 1.2rem;
      opacity: 0.8;
    }

    /* FAQ */
    .presentation-section {
      display: flex;
      justify-content: center;
      padding: 2rem;
      position: relative;
      z-index: 2;
    }

    .info-box {
      max-width: 900px;
      width: 100%;
      background: rgba(0, 0, 0, 0.6);
      border: 2px solid white;
      border-radius: 16px;
      padding: 2rem;
      box-shadow: 0 0 50px #e803b2;
      color: white;
      animation: floaty 4s ease-in-out infinite;
      transform-origin: center;
    }

    .info-item {
      border-top: 1px solid rgba(255, 255, 255, 0.3);
      padding: 1rem 0;
    }

    .info-header {
      display: flex;
      justify-content: space-between;
      cursor: pointer;
    }

    .toggle-btn {
      background: none;
      border: none;
      color: white;
      font-size: 1.5rem;
      cursor: pointer;
    }

    .info-content {
      display: none;
      margin-top: 0.75rem;
      line-height: 1.5;
    }

    .info-item.active .info-content {
      display: block;
    }

    @keyframes floaty {
      0% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-14px);
      }
      100% {
        transform: translateY(0px);
      }
    }

    .cloud-layer {
      position: fixed;
      top: 80px;
      left: 0;
      width: 100%;
      height: calc(100vh - 80px);
      pointer-events: none;
      z-index: 1;
      overflow: hidden;
    }

    .cloud {
      position: absolute;
      width: 360px;
      opacity: 0.18;
      transition: transform 0.1s linear;
    }

    .cloud-1 {
      top: 4%;
      left: -6%;
    }

    .cloud-2 {
      top: 14%;
      right: -8%;
      width: 420px;
    }

    .cloud-3 {
      top: 26%;
      left: 12%;
      width: 300px;
    }

    .footer {
      margin-top: 4rem;
      height: 80px;
      background: url("/footer.webp");
      background-size: cover;
      position: relative;
      z-index: 2;
    }

    @media (max-width: 820px) {
      .hero-video {
        padding-top: 8rem;
        padding-left: 1rem;
        padding-right: 1rem;
      }

      .hero-video-el {
        max-height: 220px;
      }

      .cloud {
        width: 220px;
      }
    }
  </style>
</Layout>
