---
import Layout from "../layouts/Layout.astro";
import Footer from "../components/Footer.astro";

interface Game {
  id: number;
  title?: string;
  img?: string;
}

const SUPABASE_URL = "https://wxjylkvjfnoijclnsffw.supabase.co";
const SUPABASE_KEY = import.meta.env.PUBLIC_SUPABASE_KEY || "sb_secret_jAuh8XhtJtcsYsH5yPDOYw_igg62J3R";

let games: Game[] = [];
let fetchError = "";

try {
  const res = await fetch(`${SUPABASE_URL}/rest/v1/games?select=*`, {
    headers: {
      apikey: SUPABASE_KEY,
    },
  });

  if (!res.ok) {
    const errorText = await res.text();
    fetchError = `Supabase fetch failed: ${res.status} - ${errorText}`;
  } else {
    const data = await res.json();
    games = Array.isArray(data) ? data : [];
  }
} catch (err) {
  fetchError = `Supabase fetch error: ${err}`;
}
---

<Layout>
  <main class="games-page reveal">
    <section class="games-header reveal">
      <h1 class="games-title">GAMES</h1>
      <p class="games-subtitle">In association with Trackman</p>
      <div class="games-icon">
        <img src="/timeglas.gif" alt="Timeglas" />
      </div>
    </section>

    <section class="games-grid reveal">
      {
        fetchError ? (
          <p class="games-empty">{fetchError}</p>
        ) : games.length === 0 ? (
          <p class="games-empty">Der er ingen games endnu – tjek Supabase tabellen.</p>
        ) : (
          games.map((game) => {
            if (!game.img) return null;

            return (
              <a href={`/${game.id}`} class="game-card">
                <img src={game.img} alt={game.title ?? "Game"} loading="lazy" />
              </a>
            );
          })
        )
      }
    </section>

    <div class="footer"></div>
  </main>

  <style>
    *,
    *::before,
    *::after {
      box-sizing: border-box;
    }

    /* ===== PAGE LAYOUT ===== */

    .games-page {
      min-height: 100vh;
      width: 100%;
      padding: 12rem 1.5rem 4rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;
    }

    .games-header {
      margin-bottom: 2.5rem;
    }

    /* H1 arver global PixelifySans */
    .games-title {
      margin: 0 0 0.5rem 0;
      font-size: 2.6rem;
      letter-spacing: 2px;
      text-transform: uppercase;
      text-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
    }

    .games-subtitle {
      margin: 0;
      font-size: 1.25rem;
      opacity: 0.9;
    }

    .games-icon {
      margin-top: 1.8rem;
      display: flex;
      justify-content: center;
    }

    .games-icon img {
      width: 80px;
      height: auto;
      opacity: 0.9;
    }

    /* ===== GRID ===== */

    .games-grid {
      margin-top: 3rem;
      width: 100%;
      max-width: 1100px;
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 2.6rem;
      justify-items: center;
    }

    /* ===== CARD ===== */

    .game-card {
      width: 100%;
      max-width: 230px;
      aspect-ratio: 3 / 4;
      border-radius: 22px;
      overflow: hidden;
      position: relative;
      background: radial-gradient(circle at top, rgba(255, 255, 255, 0.75), rgba(255, 255, 255, 0.98));
      box-shadow:
        0 0 18px rgba(255, 255, 255, 0.5),
        0 0 32px rgba(255, 255, 255, 0.35),
        inset 0 0 20px rgba(255, 255, 255, 0.1);
      display: block;
      filter: drop-shadow(0 0 12px rgba(255, 255, 255, 0.4));
      animation: floaty 4s ease-in-out infinite;
      transition:
        transform 0.3s ease,
        box-shadow 0.3s ease,
        filter 0.3s ease;
    }

    .game-card img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .game-card:hover {
      transform: translateY(-14px) scale(1.05) rotateZ(-1.5deg);
      box-shadow:
        0 0 35px rgba(232, 3, 178, 0.9),
        inset 0 0 32px rgba(255, 255, 255, 0.25);
      filter: drop-shadow(0 0 28px rgba(255, 0, 183, 0.75));
    }

    .games-empty {
      opacity: 0.8;
      font-size: 0.95rem;
    }

    .footer {
      margin-top: 4rem;
      height: 80px;
      background: url("/footer.webp");
      background-size: cover;
      position: relative;
      z-index: 2;
    }

    .footer {
      margin-top: 4rem;
      height: 80px;
      background: url("/footer.webp");
      background-size: cover;
      position: relative;
      z-index: 2;
    }


    /* ===== ANIMATION ===== */

    @keyframes floaty {
      0% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-6px);
      }
      100% {
        transform: translateY(0);
      }
    }

    /* ===== RESPONSIVE ===== */

    @media (min-width: 900px) {
      .games-grid {
        grid-template-columns: repeat(4, minmax(0, 1fr));
      }
    }

    @media (max-width: 768px) {
      .games-grid {
        gap: 3rem;
      }

      .games-page {
        padding-top: 8rem;
      }

      .games-title {
        font-size: 2.1rem;
        letter-spacing: 1px;
      }

      .games-subtitle {
        font-size: 1rem;
      }
    }

    @media (max-width: 480px) {
      .games-page {
        padding-top: 7rem;
      }

      .games-title {
        font-size: 1.8rem;
      }

      .games-subtitle {
        font-size: 0.9rem;
      }

      .games-grid {
    grid-template-columns: 1fr;   /* 👈 kun én kolonne */
    gap: 2.5rem;
  }

  .game-card {
    max-width: 100%;              /* 👈 fyld bredden */
    width: 100%;
    aspect-ratio: 3 / 4;          /* behold dit design */
  }

  .reveal {
    opacity: 1 !important;
    transform: none !important;
  }

  .games-page .reveal {
    opacity: 1 !important;
    transform: none !important;
  }



    }
  </style>
</Layout>
