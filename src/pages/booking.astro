---
import Layout from "../layouts/Layout.astro";
---

<Layout>
  <div class="cloud-layer">
    <img src="/sky.webp" class="cloud cloud-1" alt="" aria-hidden="true" />
    <img src="/sky.webp" class="cloud cloud-2" alt="" aria-hidden="true" />
    <img src="/sky.webp" class="cloud cloud-3" alt="" aria-hidden="true" />
  </div>

  <style>
    :root {
      --accent: #00eaff;
    }

    p {
      text-align: justify;
      margin-right: 10px;
      word-spacing: normal;
    } 

    .hero {
      max-width: 900px;
      margin: 0 auto;
      padding-top: 12rem;
      padding-bottom: 5rem;
      text-align: center;
      position: relative;
      z-index: 2;
    }

    .hero-text h1 {
      margin: 0 0 0.75rem 0;
      font-size: 2.8rem;
      letter-spacing: 3px;
    }

    .hero-text h2 {
      margin: 0;
      font-size: 1.25rem;
      opacity: 0.85;
      max-width: 520px;
      margin-inline: auto;
    }

    .section-title {
      margin-top: 3.5rem;
      margin-bottom: 1.2rem;
      text-align: center;
      font-size: 1.7rem;
      letter-spacing: 2px;
    }

    .booking-section {
      max-width: 920px;
      margin: 0 auto 5rem;
      display: grid;
      grid-template-columns: 1.5fr 1fr;
      gap: 3rem;
      line-height: 1.7;
      position: relative;
      z-index: 2;
    }

    .booking-left p + p {
      margin-top: 1rem;
    }

    .booking-right {
      padding: 2rem;
      border-radius: 22px;
      background: rgba(0, 0, 0, 0.35);
      border: 3px solid rgba(255, 255, 255, 0.35);
      box-shadow: 0 0 50px rgba(255, 255, 255, 0.15);
      display: flex;
      flex-direction: column;
      gap: 1.25rem;
      animation: floaty 4s ease-in-out infinite;
      transform-origin: center;
    }

    @keyframes floaty {
      0% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-6px);
      }
      100% {
        transform: translateY(0px);
      }
    }

    .booking-right h3 {
      margin: 0;
      font-size: 1.25rem;
      letter-spacing: 1.5px;
    }

    .booking-list {
      list-style: none;
      padding: 0;
      margin: 0;
      font-size: 0.95rem;
      opacity: 0.9;
    }

    .booking-list li::before {
      content: "• ";
      color: var(--accent);
    }

    .booking-list li + li {
      margin-top: 0.4rem;
    }

    .book-btn {
      display: flex;
      justify-content: flex-start;
      margin-top: 2rem;
    }

    .book-btn img {
      width: 180px;
      height: auto;
      cursor: pointer;
      transition:
        transform 0.25s ease,
        filter 0.25s ease;
      image-rendering: pixelated;
    }

    .book-btn img:hover {
      content: url("/bookhover.png");
    }

    .footer {
      margin-top: 4rem;
      height: 80px;
      background: url("/footer.png");
      background-size: cover;
      position: relative;
      z-index: 2;
    }

    .cloud-layer {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100vh;
      pointer-events: none;
      z-index: 1;
      overflow: hidden;
    }

    .cloud {
      position: absolute;
      width: 360px;
      opacity: 0.18;
      transition: transform 0.1s linear;
    }

    .cloud-1 {
      top: 4%;
      left: -6%;
    }

    .cloud-2 {
      top: 14%;
      right: -8%;
      width: 420px;
    }

    .cloud-3 {
      top: 26%;
      left: 12%;
      width: 300px;
    }

    @media (max-width: 820px) {
      .hero {
        padding-top: 8rem;
        padding-bottom: 3rem;
        padding-left: 1rem;
        padding-right: 1rem;
      }

      .hero-text h1 {
        font-size: 2.1rem;
        letter-spacing: 1px;
      }

      .hero-text h2 {
        font-size: 1rem;
      }

      .booking-section {
        grid-template-columns: 1fr;
        gap: 1.5rem;
        padding: 0 1rem;
        margin-bottom: 4rem;
      }

      .booking-right {
        padding: 1.75rem;
        margin-left: 0.75rem;
        margin-right: 0.75rem;
      }

      .book-btn {
        justify-content: center;
      }

      .cloud {
        width: 220px;
      }
    }

    @media (max-width: 480px) {
      .hero {
        padding-top: 7rem;
      }

      .hero-text h1 {
        font-size: 1.8rem;
      }

      .hero-text h2 {
        font-size: 0.9rem;
      }

      .booking-right h3 {
        font-size: 0.5rem;
      }

      .book-btn {
        justify-content: center;
      }

      .book-btn a {
        width: 100%;
      }
    }
  </style>

  <section class="hero reveal">
    <div class="hero-text">
      <h1>BOOK DIN TID</h1>
      <h2>Simulatorgolf, minigames og klubstemning klar til booking.</h2>
    </div>
  </section>

  <h2 class="section-title reveal">SÅDAN BOOKER DU</h2>

  <section class="booking-section reveal">
    <div class="booking-left reveal">
      <p>Du booker din tid hos The Clubhouse simgames via Simgolfs bookingsystem. Her vælger du nemt tidspunkt, antal spillere og hvor længe I ønsker at spille.</p>

      <p>Når I ankommer, står vi klar til at hjælpe jer i gang med simulatoren, minigames og eventuelle pakker. I skal bare møde op, tage vennerne med og være klar på en aften fyldt med grin, konkurrence og afslappet klubstemning.</p>

      <p>Klik på knappen herunder for at gå direkte til booking. Selve bookingen foregår på Simgolfs hjemmeside – men oplevelsen er den samme: én samlet aften med golf, games og gode vibes.</p>
      <div class="book-btn">
        <a href="https://www.simgolf.dk/booking" target="_blank" rel="noreferrer"><img src="/book.webp" alt="Book knap" /></a>
      </div>
    </div>

    <div class="booking-right reveal">
      <h3>Praktisk info</h3>
      <ul class="booking-list">
        <li>Vælg nemt tid og simulator direkte i bookingen</li>
        <li>Perfekt til mindre grupper, dates og hyggelige venneaftener</li>
        <li>Mulighed for drikkevarer og afslappet hygge på stedet</li>
        <li>TrackMan-minigames og challenges står klar, når I gør</li>
      </ul>

      <h3>Tip til en god aften</h3>
      <ul class="booking-list">
        <li>Kom gerne 10–15 minutter før, så I er klar fra første slag</li>
        <li>Overvej på forhånd hvilke minigames I vil kaste jer over</li>
        <li>Aftal om I spiller for sjov – eller om der skal være noget på spil</li>
      </ul>
    </div>
  </section>

  <div class="footer"></div>
  <script is:inline>
    (() => {
      const clouds = document.querySelectorAll(".cloud");
      if (!clouds.length) return;

      const maxMove = window.innerWidth + 600;

      window.addEventListener("scroll", () => {
        const progress = Math.min(window.scrollY / 600, 1);

        clouds.forEach((cloud, i) => {
          const direction = i % 2 === 0 ? -1 : 1;
          const offset = progress * maxMove;

          cloud.style.transform = `translateX(${direction * offset}px)`;
        });
      });
    })();
  </script>
</Layout>
